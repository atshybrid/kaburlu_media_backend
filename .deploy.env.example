# Copy to .deploy.env in the repo root (gitignored) so `npm run deploy:droplet` works without exports.
DEPLOY_HOST=
DEPLOY_SSH_KEY=

# Optional overrides:
# DEPLOY_USER=deploy
# DEPLOY_PATH=/home/deploy/kaburlu_media_backend
# DEPLOY_APP_NAME=kaburlu-api
