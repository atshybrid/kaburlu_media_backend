# ---------------- Database ----------------
# Neon requires SSL. Ensure `sslmode=require` is present.
# Runtime can use pooler (`-pooler` + `pgbouncer=true`).
# Migrations must use a direct (non-pooler) connection via `DATABASE_URL_DIRECT`.

# Option A (simple): set DATABASE_URL (+ DATABASE_URL_DIRECT for Neon migrations)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require&pgbouncer=true
DATABASE_URL_DIRECT=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# Option B (recommended): keep multiple profiles and select with DB_PROFILE
# DB_PROFILE=dev | prod | local
DB_PROFILE=local
DATABASE_URL_LOCAL=postgresql://postgres:postgres@localhost:5432/kaburlu_local?sslmode=disable

# Example Neon (dev)
DATABASE_URL_DEV=postgresql://USER:PASSWORD@YOUR-NEON-DB-pooler.neon.tech/kaburlu_dev?sslmode=require&pgbouncer=true
DATABASE_URL_DIRECT_DEV=postgresql://USER:PASSWORD@YOUR-NEON-DB.neon.tech/kaburlu_dev?sslmode=require

# Example Neon (prod)
DATABASE_URL_PROD=postgresql://USER:PASSWORD@YOUR-NEON-DB-pooler.neon.tech/kaburlu_prod?sslmode=require&pgbouncer=true
DATABASE_URL_DIRECT_PROD=postgresql://USER:PASSWORD@YOUR-NEON-DB.neon.tech/kaburlu_prod?sslmode=require

# Prefer fallback only in development. Set to true to use DATABASE_URL_FALLBACK.
PRISMA_PREFER_FALLBACK=false
# DATABASE_URL_FALLBACK=postgresql://postgres:postgres@localhost:5432/kaburlu_local?sslmode=disable


# ---------------- Server ----------------
# For development, allow all origins:
CORS_ALLOW_ALL=true
# For production, comment out CORS_ALLOW_ALL and use CORS_ORIGINS:
# CORS_ORIGINS=https://yourdomain.com,http://localhost:3000
# ---------------- App Config ----------------
JWT_SECRET=thisissecret
NODE_ENV=production
BASE_URL=http://localhost:3000
MULTI_TENANCY=true

# ---------------- Razorpay ----------------
# RAZORPAY_KEY_ID=rzp_test_npGPBei3cuO4vw
# RAZORPAY_KEY_SECRET=bq3G4sgMEQ5cKoPnOwCCHmsV
# RAZORPAY_WEBHOOK_SECRET=whsec_test_1234567890abcdef

# ---------------- Translation ----------------
GEMINI_API_KEY=REPLACE_WITH_GEMINI_API_KEY

AI_SERVICE_URL=https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash-latest:generateContent


# ---------------- Firebase ----------------
FIREBASE_CREDENTIALS_PATH="./secrets/firebase-service-account.json"
FIREBASE_PROJECT_ID=kaburlu-2f6de
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-fbsvc@kaburlu-2f6de.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="REPLACE_WITH_FIREBASE_PRIVATE_KEY"

# ---------------- Youtube ----------------
YOUTUBE_API_KEY=REPLACE_WITH_YOUTUBE_API_KEY


# ---------------- OpenAI ----------------

OPENAI_API_KEY=REPLACE_WITH_OPENAI_API_KEY

# ---------------- Cloudflare R2 ----------------
R2_ACCOUNT_ID=REPLACE_WITH_R2_ACCOUNT_ID
R2_ACCESS_KEY_ID=REPLACE_WITH_R2_ACCESS_KEY_ID
R2_SECRET_ACCESS_KEY=REPLACE_WITH_R2_SECRET_ACCESS_KEY
R2_BUCKET=REPLACE_WITH_R2_BUCKET
R2_PUBLIC_BASE_URL=REPLACE_WITH_R2_PUBLIC_BASE_URL

# ---------------- Media Provider ----------------
# r2 | bunny
MEDIA_PROVIDER=r2

# ---------------- Bunny Storage (Images/Files) ----------------
# https://bunny.net/storage
BUNNY_STORAGE_ZONE_NAME=REPLACE_WITH_BUNNY_STORAGE_ZONE_NAME
BUNNY_STORAGE_API_KEY=REPLACE_WITH_BUNNY_STORAGE_API_KEY
# Public base URL (usually your Pull Zone / CDN URL), e.g. https://yourzone.b-cdn.net
BUNNY_STORAGE_PUBLIC_BASE_URL=REPLACE_WITH_BUNNY_STORAGE_PUBLIC_BASE_URL

# ---------------- Bunny Stream (Videos) ----------------
# https://bunny.net/stream
BUNNY_STREAM_LIBRARY_ID=REPLACE_WITH_BUNNY_STREAM_LIBRARY_ID
BUNNY_STREAM_API_KEY=REPLACE_WITH_BUNNY_STREAM_API_KEY
# Optional (defaults to https://iframe.mediadelivery.net/embed)
BUNNY_STREAM_EMBED_BASE_URL=https://iframe.mediadelivery.net/embed

# ---------------- Media Upload Limits ---------------- 

MEDIA_MAX_IMAGE_MB=10
MEDIA_MAX_VIDEO_MB=100

# ---------------- SEO Config ----------------

SEO_PUBLISHER_NAME=Kaburlu #(e.g., “HRCI Today News”)
SEO_PUBLISHER_LOGO=https://pub-b13a983e33694dbd96cd42158ce2147b.r2.dev/SEO_PUBLISHER_LOGO/2025/09/13/SEO_PUBLISHER_LOGO.png #(absolute URL to a square logo, preferably 60x60 to 600x60)

# ---------------- Slug Config ----------------

SLUG_REQUIRE_UNIQUE=true # when true slug uniqueness is enforced; timestamp suffix skipped

# ---------------- AI Config ----------------

## ---------------- AI Provider Selection ----------------
# Single authoritative provider value (gemini | openai)
AI_PROVIDER=gemini
# Example alternative:
# AI_PROVIDER=openai
AI_USE_GEMINI=true
AI_USE_OPENAI=false

AI_ENABLE_SEO=true 
AI_ENABLE_MODERATION=true 
AI_ENABLE_TRANSLATION=true

GEMINI_MODEL_SEO=gemini-1.5-flash
GEMINI_MODEL_MODERATION=gemini-1.5-flash
GEMINI_MODEL_TRANSLATION=gemini-1.5-pro
OPENAI_MODEL_SEO=gpt-4o-mini
OPENAI_MODEL_MODERATION=gpt-4o-mini
OPENAI_MODEL_TRANSLATION=gpt-4o-mini

AI_TIMEOUT_MS=500000


ALLOW_DESTRUCTIVE=true # set to false to prevent destructive actions (e.g., deleting tenants)